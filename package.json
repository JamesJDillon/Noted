{
  "name": "markdown-blog",
  "version": "0.0.1",
  "description": "Small tool for generating static blog pages from markdown.",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.2"
  },
  "dependencies": {
    "pretty": "^2.0.0",
    "showdown": "^1.9.1"
  }
}
